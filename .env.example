# HTTP port for production (npm start)
PORT=3000

# Dev-only WebSocket relay port (Vite proxies /ws here)
WS_PORT=3001

# Optional: full WebSocket URL if WS is not on the same host as the app
# VITE_WS_URL=wss://your-domain.com/ws
